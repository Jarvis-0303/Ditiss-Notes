Optionally select:
  Add  a README file
  add .gitignore
  choose a licence
  click create repo


Git commands 
git add
git commit 
git push


To setup git to work with GitHub
Git config --global user.name "your git id"
git config --global user.email "your email_id"

To genrate 
